nclude<stdio.h>
void main()
{
	int n,r,sum=0;
	printf("\n ENTER ANY DIGIT");
	scanf("%d",&n);
	while(n>0)
	{
		r=n%10;
		sum=(sum*10)+r;
		n=n/10;
	}
	printf("Reverse of a number = %d",sum);
	getch();
}
